#include<bits/stdc++.h>
using namespace std;

int main()
{
    unordered_set<int>st;
    // lower_bound and upper_bound functions does not works
    // rest all functions are same as above
    // it does not stores in any particular oredr 
    // it has a better complexity than set in most cases, except when collisan happens
    return 0;
}
